# it works here if i make it scope test, and then the app doesnt work the not testing part
# but if i remove the scope it works for both, apparently

# chnge the scope for test and this seems to be pickedup but it doesn work i can comment it out and still use the db,
# and in this case it doesnt work for the runtime
spring.datasource.url=jdbc:h2:mem:dbTest
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
